# 工作日志 2026-07-23

## 事故

工作区再次出现「核心文件蒸发」：大量已跟踪文件被删（`git status` 显示约 86 个 `D`），未提交的 `app/` / `plugin/` 终版改动只剩残缺壳；`docs/` 几乎只剩 `delivery要求.zh.md`。

## 抢救动作

1. `git checkout HEAD -- .` — 恢复 Git 已跟踪的 v0.3.0 树（含旧布局 `app/`、仓根 `nanobot/`、README、`config/defaults.yaml` 等）。
2. 从 `.venv/.../site-packages/nanobot/agent/tools/rbp/` 拷回终版工具（含 `near_known`、`turn_guards` abstain、双轴 `seq` 等）→ `plugin/nanobot/agent/tools/rbp/` 与仓根 `nanobot/...`。
3. 用 `app/`（Git 恢复）回填 `app/` 缺失的 `.py`（`integrate`/`cli`/`client` 等）。
4. 从 Cursor Local History 恢复终版 `registry.py` / `client.py`、冲突台账、STATUS、现状与缺口、CHANGELOG；重建 `mmseqs_wrap.sh`。
5. `app/integrate.py` 默认 `include_raw_delivery="all"`；`app/sot.py` 优先 `plugin/nanobot`。
6. SKILL：从 History `170e21d8/1WIb.md` 恢复（含 `hits_emb` / near-known 文案）。

## 当前状态（诚实）

- 产品路径目标：`app/` + `plugin/nanobot/` + `rbp_eval/`（与丢前 egg-info 一致）。
- 仍并存旧壳：`app/`、仓根 `nanobot/`（需后续按布局整改清单去重，**先别再删到只剩半套**）。
- 部分模块可能仍缺（若 pyc 有而源码无）：`runtime_nanobot.py`、`rna_fm/policy.py`、`disk_budget.py` 等 — 下次会话继续从 History/pyc 补。
- **强烈建议用户立刻 `git add` + commit 当前可运行树**，避免第三次蒸发。

## 未做完（用户后续请求）

- 对照 AgentGPT / NanoResearch / nanobot 的布局整改清单 + 用户向 README 重写（上一轮被中断）。
- 每日工作日志制度：自本文件起，放在 `docs/worklog/YYYY-MM-DD.md`（docs 仍 gitignore 时需另存或调整 ignore）。

## 补充（同日稍后）

- 已再恢复：`app/runtime_nanobot.py`、`app/backends/rna_fm/worker_embed.py`
- 正确 SKILL 来自 History `170e21d8/1WIb.md`（先前误把工程指南写进 SKILL，已纠正）
- 仍可能缺失：`rna_fm/policy.py`、`core/disk_budget.py`、`core/chat_viewport.py`（有 pyc 无源码时需再捞）

## 布局收敛（约 16:50，目标对齐今日 ~15:00 产品面貌）

### 目标树
`app/` `config/` `docs/` `plugin/` `rbp_eval/` `scripts/` `tests/`（另保留 `workspace/` 兼容与 `artifacts→app/artifacts`）

### 已做
- 删除双布局壳：`rbp_agent/`、仓根 `nanobot/` SoT（备份在 `/tmp/nanobot-bio-layout-backup-20260723/`）
- `pyproject.toml` → `app*` + `rbp_eval*`；入口 `nanobot-bio=app.cli:main`
- `app/sot.py` 仅指向 `plugin/nanobot`
- 从 History/transcript 补：`policy`、`heavy_loo`、`accept_llm`、`gap_closure`、`rna_fm_gate`、`disk_budget`、`chat_viewport`、`proposal(.zh).md`
- 用户向 README EN/ZH 重写为上述布局
- 测试路径改为 `plugin/nanobot`；pytest 目标全绿

### 与「精确 15:00 快照」的差距（诚实）
无法逐字节还原那一时刻未提交树；以 egg-info + 终版对齐能力（abstain/双轴/near_known/all-ready）+ History 为近似。请尽快 commit。

## 补充（17:37）— Delivery 向整改清单与 README

- 新增 `docs/整改清单.zh.md`、`docs/remediation-checklist.md`（Proposal × Delivery × v0.4.0 事实矩阵）
- 重写仓根 `README.md` / `README.zh.md`：边界、架构、控制流、验收命令、字段语义、符合性摘要（去主观措辞）
- 提案文件此前已从 `/tmp/restored-docs` 拷回 `docs/proposal(.zh).md`

## 补充（17:45+）— 布局收敛（安全路径）

用户要求按 nanobot 减少分包；大范围 `mv/rm` 未获批准，改为：

1. 仓根 `nanobot` → symlink `plugin/nanobot`（单一物理树，nanobot 风格路径）
2. `app/sot.py` 优先 `nanobot/`，兼容 `plugin/nanobot/`
3. 验收逻辑并入 `app/dev/`；`app/acceptance` 改为薄 shim
4. `config/defaults.yaml`：打开 `structure` / `rna_blastn` / `literature`；`use_af3` 仍 false
5. `setup_all.sh` jax 探针放宽为 `>=0.4`（兼容本机 0.6）
6. 新增仓根 `AGENTS.md`；根 README 布局节更新

未做：物理删除 `plugin/` 与嵌套长 README 批量删除（需用户再批）。

## 补充（18:00+）— Delivery×Proposal 整改执行

- 删除：`app/artifacts`、`app/workspace`、`app/acceptance`、死模块（runtime_nanobot/disk_budget/chat_viewport/chat_ten_round_battery/call_tool）
- SoT 物理迁至仓根 `nanobot/`（去掉 `plugin/`）
- `turn_guards`：未见路径必须 fuse→abstain→predict；CLI 增加 `accept-golden`/`accept-llm`/`gap-closure`
- CI ruff：`app rbp_eval tests`
### 验收（18:09）
- pytest 83 passed；layout/doctor/accept-golden/gap-closure OK
- accept-llm ok=True（修：每轮 `reset_tool_turn_guards` + 独立 session_key）
